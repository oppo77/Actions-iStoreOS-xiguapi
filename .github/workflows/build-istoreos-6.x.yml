name: Build iStore OS 6.x

on:
  repository_dispatch:
  workflow_dispatch:
  schedule:
   - cron: 0 16 * * *

env:
  REPO_URL: https://github.com/istoreos/istoreos
  REPO_BRANCH: istoreos-24.10.2
  FEEDS_CONF: feeds.conf
  CONFIG_FILE: .config
  DIY_P1_SH: diy-part1-6.x.sh
  DIY_P2_SH: diy-part2-6.x.sh
  UPLOAD_BIN_DIR: true
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  ARCHITECTURE: armv8
  # 保留OS_VERSION但runs-on直接写固定值，避免解析问题
  OS_VERSION: ubuntu-22.04

jobs:
  build:
    # 直接写固定值，解决env引用解析问题
    runs-on: ubuntu-22.04
    # 移除无效的fail-fast（无matrix时该属性无意义）

    steps:
    - name: 合并磁盘
      uses: easimon/maximize-build-space@master
      with:
        swap-size-mb: 1024
        temp-reserve-mb: 100
        root-reserve-mb: 1024

    - name: 准备完成
      uses: actions/checkout@main

    - name: 创建工作目录
      run: |
        mkdir -p ${GITHUB_WORKSPACE}/workdir
        chmod 755 ${GITHUB_WORKSPACE}/workdir
        echo "WORKDIR=$PWD" >> $GITHUB_ENV

    - name: 检查服务器配置
      run: |
        echo "若分配的服务器性能不足，务必及时取消，重新运行！"
        echo -e "------------------------------- CPU信息 -------------------------------\n"
        echo "CPU物理数量:$(cat /proc/cpuinfo| grep "physical id"| sort| uniq| wc -l)"
        echo -e "CPU核心及版本信息: $(cat /proc/cpuinfo | grep name | cut -f2 -d: | uniq -c) \n"
        echo -e "------------------------------- 内存信息 -------------------------------\n"
        echo "已安装内存详细信息: "
        sudo lshw -short -C memory | grep GiB
        echo -e "\n"
        echo -e "------------------------------- 磁盘信息 -------------------------------\n"
        echo -e "磁盘数量: $(ls /dev/sd* | grep -v [1-9] | wc -l) \n"
        echo "------------------------------- 磁盘详情 -------------------------------\n"
        df -Th

    - name: 初始化编译环境
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        echo "------------------------------- 系统更新升级 -------------------------------"
        sudo apt update -y && sudo apt upgrade -y
        
        echo "------------------------------- 安装编译核心依赖 -------------------------------"
        sudo apt install -y \
          build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
          autoconf automake binutils libtool patch pkgconf quilt \
          gettext libncursesw5-dev libncurses-dev libreadline-dev \
          libssl-dev libelf-dev libglib2.0-dev device-tree-compiler libcrypt-compat \
          liblzma-dev libc6-dev-i386 lib32gcc-s1 lib32stdc++6 zlib1g-dev \
          python3 python3-pyelftools python3-setuptools python3-distutils \
          python3-pip python3-yaml python3-dev libpython3-dev \
          bzip2 ccache cpio curl rsync squashfs-tools unzip xxd bc haveged \
          uuid-runtime dosfstools mtools parted u-boot-tools \
          qemu-utils vim subversion git xsltproc libxml-parser-perl mercurial \
          bzr ecj cvs ant wget cmake ninja-build patchelf swig \
          openjdk-11-jre-headless \
          apt-utils rsync curl wget sudo uuid-runtime
        
        echo "------------------------------- 清理冗余依赖/镜像 -------------------------------"
        docker rmi `docker images -q` || true
        docker image prune -a -f || true
        sudo apt -y purge azure-cli ghc* zulu* hhvm llvm* firefox google* dotnet* \
          powershell openjdk-17-jre-headless mysql* php* mongodb* moby* snapd* android* || true
        sudo apt autoremove --purge -y
        sudo apt clean -y
        
        echo "------------------------------- Python工具安装 -------------------------------"
        sudo pip3 install pyelftools setuptools yaml
        
        echo "------------------------------- 设置时区 -------------------------------"
        sudo timedatectl set-timezone "$TZ"

    - name: 克隆源码
      working-directory: ${{ env.WORKDIR }}/workdir
      run: |
        df -hT $PWD
        git clone $REPO_URL -b ${{ env.REPO_BRANCH }} openwrt
        ln -sf ${{ env.WORKDIR }}/workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: 执行xiguapi-v3设备适配脚本
      run: |
        SCRIPT_PATH="${GITHUB_WORKSPACE}/xiguapi/xiguapi-v3-adapt.sh"
        chmod +x $SCRIPT_PATH
        $SCRIPT_PATH
        if [ $? -eq 0 ]; then
          echo "✅ xiguapi-v3适配脚本执行成功"
        else
          echo "❌ xiguapi-v3适配脚本执行失败"
          exit 1
        fi

    - name: 加载自定义 feeds
      env:
        FEEDS_CONF_PATH: ${{ env.ARCHITECTURE }}/${{ env.FEEDS_CONF }}
      run: |
        [ -e $FEEDS_CONF_PATH ] && mv $FEEDS_CONF_PATH openwrt/$FEEDS_CONF
        chmod +x $DIY_P1_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P1_SH

    - name: 更新 feeds（第1遍）
      run: cd openwrt && ./scripts/feeds update -a

    - name: 更新 feeds（第2遍）
      run: cd openwrt && ./scripts/feeds update -a

    - name: 安装 feeds（第1遍）
      run: cd openwrt && ./scripts/feeds install -a

    - name: 安装 feeds（第2遍）
      run: cd openwrt && ./scripts/feeds install -a

    - name: 加载自定义配置
      env:
        CONFIG_FILE_PATH: ${{ env.ARCHITECTURE }}/${{ env.CONFIG_FILE }}
      run: |
        [ -e files ] && mv files openwrt/files
        [ -e $CONFIG_FILE_PATH ] && mv $CONFIG_FILE_PATH openwrt/$CONFIG_FILE
        chmod +x $DIY_P2_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: 下载软件包（第1遍）
      id: package1
      run: |
        cd openwrt
        make defconfig
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: 下载软件包（第2遍）
      id: package2
      run: |
        cd openwrt
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: 编译固件
      id: compile
      run: |
        cd openwrt
        echo -e "$(nproc) thread compile"
        make -j$(nproc) || make -j1 || make -j1 V=s
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV
        echo "status=success" >> ${GITHUB_OUTPUT}

    - name: 检查磁盘空间
      if: (!cancelled())
      run: df -hT

    - name: 上传 bin 文件夹
      uses: actions/upload-artifact@main
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      with:
        name: OpenWrt_bin${{ env.FILE_DATE }}
        path: openwrt/bin

    - name: 整理固件文件
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> ${GITHUB_OUTPUT}

    - name: 上传固件目录
      uses: actions/upload-artifact@main
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_firmware${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE }}

    - name: 生成发布标签
      id: tag
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_RELEASE == 'true' && !cancelled()
      run: |
        echo "release_tag=$(date +"%Y.%m.%d-%H.%M")-${{ env.ARCHITECTURE }}-xiguapi-v3" >> ${GITHUB_OUTPUT}
        touch release.txt
        echo "
        💻 架构: ${{ env.ARCHITECTURE }}
        📇 设备: Xiguapi V3 (rk3568)
        📂 源码: ${{ env.REPO_URL }}
        🌳 分支: ${{ env.REPO_BRANCH }}
        ⏱️ 编译时间: $(date +"%Y年%m月%d日%H时%M分")
        🌐 管理地址: 192.168.100.1
        👤 用户名: root
        🔒 密码: password
        📒 说明: Xiguapi V3 设备专属固件，默认网口 eth0=LAN, eth1=WAN，主路由模式。 " >> release.txt
        echo "status=success" >> ${GITHUB_OUTPUT}

    - name: 自动发布固件到 Releases
      uses: softprops/action-gh-release@v2
      if: steps.tag.outputs.status == 'success' && !cancelled()
      with:
        tag_name: ${{ steps.tag.outputs.release_tag }}
        body_path: release.txt
        files: ${{ env.FIRMWARE }}/*

    - name: 删除运行记录
      uses: xiaomeng9597/delete-workflow-runs@main
      with:
        retain_days: 20
        keep_minimum_runs: 15
        token: ${{ env.GITHUB_TOKEN }}

    - name: 删除自动发布的旧固件
      uses: dev-drprasad/delete-older-releases@v0.3.3
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        keep_latest: 60
        delete_tags: true
